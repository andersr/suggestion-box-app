<%= form_for [@suggestion_box, @suggestion] do |f| %>
  <% if @suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% @suggestion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :suggestion_message %><br />
    <%= f.text_area :suggestion_message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
